[project]
name = "tts-server"
version = "0.1.0"
description = "IndexTTS HTTP server for tachi device channel (kokoro-compatible /tts API)"
requires-python = ">=3.11,<3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]

[tool.uv]
package = false
