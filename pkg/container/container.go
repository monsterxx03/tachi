// Package container provides the shared data structures used across tachi:
// a generic concurrent map (LockedMap), a circular byte buffer (RingBuf),
// a fuzzy path-completion trie (PathTrie), and a generic set (Set).
package container
